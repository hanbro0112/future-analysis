[project]
name = "price-listener"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-dotenv>=1.2.2",
    "shioaji>=1.3.3",
    "google-cloud-pubsub>=2.30.0",
]

[project.scripts]
main = "price_listener:main"

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"
