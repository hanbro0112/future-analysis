[project]
name = "pubsub"
version = "0.1.0"
description = "GCP Pub/Sub 客戶端模組 - 提供 Publisher 和 Subscriber 功能"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "google-cloud-pubsub>=2.38.0",
]
